langcode: en
status: true
dependencies:
  config:
    - user.role.curator
  module:
    - user
id: user_add_role_action.curator
label: 'Add the Curator role to the selected users'
type: user
plugin: user_add_role_action
configuration:
  rid: curator
