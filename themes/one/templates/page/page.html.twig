{#
/**
 * @file
 * Template for the main page.
 */
#}
<div class="page">
  {% embed '@one/navbar/navbar.twig' with {
    placement: 'sticky-top',
    container: 'fixed',
  } %}

    {% block branding %}
      {% if page.navbar_branding %}
        {{ page.navbar_branding }}
      {% endif %}
    {% endblock %}

    {% block left %}
      {% if page.navbar_left %}
        <div class="mr-auto">
          <div class="navbar-left">
            {{ page.navbar_left }}
           </div>
        </div>
      {% endif %}
    {% endblock %}

    {% block right %}
      {% if page.navbar_right %}
        <div class="navbar-right">
          {{ page.navbar_right }}
          </div>
      {% endif %}
    {% endblock %}
  {% endembed %}

  <main class="main">
    {% if page.header %}
      <header class="page__header mb-3">
        <div class="container">
          {{ page.header }}
        </div>
      </header>
    {% endif %}

    {% if page.content %}
      <div class="page__content">
          {{ page.content }}
      </div>
    {% endif %}
  </main>

  {% if page.footer %}
    <footer class="page__footer">
      <div class="container">
        <div class="d-flex justify-content-md-between align-items-md-center">
          {{ page.footer }}
        </div>
      </div>
    </footer>
  {% endif %}
</div>
